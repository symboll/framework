### svelte
```Shell
npx degit sveltejs/template svelte-app-ts
node scripts/setupTypeScript.js
```

### vite
```Shell
yarn create vite
```

### Angular
```Shell
ng new project-name
ng g component | service | class | route | directive | pipe --no-spec
```

### todoList
```Shell
upgrade vue | svelte
add Rxjs to Angular
```